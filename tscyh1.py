import streamlit as st
from zhipuai import ZhipuAI

# ================= 配置区域 =================
# ⚠️ 填入你的新 Key
API_KEY = "c2cd9f6ca9394c5c9284d5d547cb5cc4.yJxCm9sJ8tR11N3H"
# ===========================================

# 初始化客户端
try:
    client = ZhipuAI(api_key=API_KEY)
except:
    pass


def generate_three_variations(user_input):
    """
    电商详情页专用 v4.1 (增加动作库限制)：
    - 严格执行衣服不掖裤腰
    - 严格限定下装补全类型
    - 细分角度 (1/5, 2/5...)
    - 强制白天光线
    - 新增：动作参考库 (Pose Library)
    """
    if not API_KEY or "我的API" in API_KEY:
        return ["❌ 错误：请在代码中配置正确的 API Key"] * 3

    # 1. 背景库
    bg_library = """
    - **纯色类**：白色、浅米色、浅灰色、深灰色。
    - **自然场景**：公园草坪、山野营地、海边沙滩、山顶平台、森系草坪、野餐垫周边、露营帐篷区、户外野餐区、郊野野餐点。
    - **生活/城市**：室内、花园草坪、校园草坪、ins风网红镜子、文艺木质书架、ins风北欧风落地窗、ins风清新花艺角落、ins风浪漫纱帘背景、ins风日式榻榻米角落。
    """

    # 2. 角度库 (极度细分，拒绝死板)
    angle_library = """
    [ 正面平拍、四分之三侧拍、微侧正面、纯侧面拍、
      背影正拍、背影侧拍、中景侧拍、近景平拍、特写平拍、
      全景平拍、侧脸平拍、半侧背影拍、中景正拍、近景侧拍、
      特写侧拍、全景侧拍、半身正拍、半身侧拍、
      七分身正拍、七分身侧拍 ]
    """

    # 3. 下装补全库 (严格限定)
    bottoms_library = "[ 裙子, 阔腿裤, 直筒裤, 工装裤, 微喇裤, 西装裤, 打底裤 ]"

    # 4. 动作库 (新增：您提供的动作列表)
    pose_library = """
    [ 单手插兜, 双手抱胸, 歪头浅笑, 回眸, 抬手撩发, 撑脸歪头, 比耶甜笑, 托腮眨眼, 插兜歪身, 搭肩歪头,
      摊手歪笑, 抱臂歪头, 抬手比心, 托腮浅笑, 撑墙歪身, 插兜抬下巴, 单手背身, 歪身撩衣摆, 抬手遮阳光,
      扶帽檐歪头, 抱膝蹲坐, 跪坐撑地, 盘腿托腮, 侧坐撑脸, 蹲姿比耶, 跪姿撩发, 站定提裙摆, 反手比心,
      歪头捂嘴笑, 单手扶腰, 单手插裤兜, 耸肩歪笑, 抬手碰耳环, 撑腰抬下巴, 侧站撩头发, 回头挥手,
      抱臂抬眼, 插兜歪头笑, 托腮歪头笑, 抬手整理衣领, 撑墙托腮, 歪身搭栏杆, 蹲姿托腮, 跪姿比耶,
      站定撩袖口, 反手撩发, 扶腰歪笑, 插兜侧站, 抬手轻触嘴唇, 歪身靠立柱, 抬手撑头顶, 叉腰抬下巴,
      抱臂歪身, 摊手歪头, 抬手拨刘海, 侧坐搭膝盖, 蹲姿撑地笑, 跪姿托下巴, 站定拽衣角, 反手搭腰,
      歪头轻扬手, 单手撑墙, 插兜抬脚跟, 抱臂抿嘴笑, 托腮抬眼望, 撑脸歪身笑, 抬手轻扶肩,
      歪身碰衣兜, 蹲姿歪头笑, 跪姿撑膝站, 站定撩衣摆, 反手碰发梢, 歪头轻托腮, 单手插裤兜, 单手扶腰笑,
      插兜歪身靠, 抱臂侧头望, 托腮歪身笑, 撑墙抬下巴, 抬手轻撩发, 歪身搭椅背, 蹲姿撑腿笑, 跪姿歪头撩发,
      站定提衣角, 反手比耶, 歪头轻挥手, 单手撑腰, 单手搭栏杆, 插兜抬眼笑, 抱臂歪头笑, 歪身插兜撩发, 
      轻倚墙面歪头, 抬手挡脸浅笑, 插兜抬脚跟歪站, 撑腰歪身抿嘴, 托腮歪头眨眼, 摊手歪笑侧站, 扶帽檐轻扬下巴, 
      反手搭腰回眸, 撩衣摆歪身笑, 撑栏杆低头浅笑, 抬手碰发梢歪头, 插兜侧走回眸, 托腮撑桌歪身, 轻倚歪站, 
      抬手整理衣角歪头, 抱臂歪身抬眼, 撩袖口歪头浅笑, 撑墙抬手撩发, 插兜歪头轻扬手]
    """

    # 5. 核心 System Prompt
    system_instruction = f"""
    你是一位电商视觉总监。用户输入原始提示词，请**一次性生成 3 个优化后的提示词方案**。

    **原始提示词**：{user_input}

    请严格执行以下【九大电商铁律】：

    1. **光线与时间 (Time Lock)**：
       - **铁律**：除非原始提示词明确提到了“夕阳”、“晚霞”、“夜晚”，否则**绝对禁止**生成黄昏或夜景！
       - **默认**：必须是**光线充足的白天 (Daylight, High-key)**。模特身上**少高光**。

    2. **上衣穿搭 (Top Styling)**：
       - **铁律**：除非原始提示词明确说“掖进裤子/塞衣角”，否则**衣服下摆必须自然垂落 (Untucked / Natural drop)**，**不要**掖在裤子里。

    3. **下装补全 (Bottoms Logic)**：
       - **铁律**：如果原始提示词没有指定裤子/下装，只能从以下列表中选择搭配：
         {bottoms_library}
       - **完整性**：模特必须穿裤子或裙子，严禁下半身失踪。

    4. **鞋子搭配**：
       - 凡是生成带脚部的图像，**必须穿鞋**。根据衣服风格搭配（运动/休闲/潮牌/流行）。

    5. **模特规范**：
       - **身份**：**中国模特 (Chinese Model)**。
       - **性别**：**如果原始提示词明确提到了性别，就按照原始提示词的性别，否则根据服装自动判断，但必须有性别。**
       - **男模**：**不化妆、没有胡子 (Clean-shaven)**。

    6. **构图逻辑**：
       - 上衣 -> 露上半身或全身。裤子 -> 露下半身或全身。套装 -> 全身。

    7. **机位与动作 (关键)**：
       - **角度**：3个方案尽量使用不同的角度(除非原始提示词指定)，必须参考：{angle_library}。
       - **动作 (Pose)**：**如果原始提示词没有规定动作，请务必从以下【动作参考库】中随机选择生动姿态，不要死板站立：**
         {pose_library}
       - 请在描述中准确使用如“五分之二侧面”、“歪头浅笑”、“插兜歪身”等词汇。
       - 动作不能有冲突，如“双手插兜”和“单手扶腰”不能同时出现，检查模特动作的合理性。

    8. **背景与画质**：
       - 参考 {bg_library}。3个方案的背景不要太单一（除非原始提示词指定）。
       - **虚化控制**：**轻度虚化 (Slight Depth of Field)**，保留环境轮廓，**拒绝**强烈大光圈虚化。
       - **画质**：固定为4K高清
       
    9. **风格**：
       - **风格偏向于ins风格，也可以偏向于淘宝服装详情页模特风格**
       
    10. **避坑**：
       - 画面中**绝对不要出现**打光灯、反光板等摄影设备。
       
    **输出格式**：
    请直接输出 3 段内容，每段之间用 "|||" 符号分隔。
    每段内容是一段连贯的中文描述，不要加标题。
    """

    try:
        response = client.chat.completions.create(
            model="glm-4",
            messages=[
                {"role": "system", "content": system_instruction},
                {"role": "user", "content": f"请为【{user_input}】生成3个详情页拍摄方案"}
            ],
            temperature=0.85,
            top_p=0.9
        )
        content = response.choices[0].message.content
        return [v.strip() for v in content.split("|||") if v.strip()]
    except Exception as e:
        return [f"API 调用出错: {e}"] * 3


# ================= 🎨 Streamlit UI (并排大框版) =================

st.set_page_config(
    page_title="电商详情页生图工具",
    page_icon="🛍️",
    layout="wide"  # 👈 宽屏模式
)

# CSS 深度优化：让 st.code 自动换行，且框足够大
st.markdown("""
<style>
    #MainMenu {visibility: hidden;}
    footer {visibility: hidden;}

    /* 优化代码块显示：强制换行，字体适配中文 */
    code {
        font-size: 15px !important; 
        font-family: "Microsoft YaHei", sans-serif !important;
        white-space: pre-wrap !important; /* 关键：强制换行 */
    }

    /* 增大复制按钮，方便点击 */
    button[title="Copy to clipboard"] {
        transform: scale(1.2); 
    }

    /* 调整列间距 */
    div[data-testid="column"] {
        padding: 0px 8px;
    }
</style>
""", unsafe_allow_html=True)

# 标题区
st.title("🛍️ 电商模特提示词优化器 (Pro)")
st.caption("✨ 特性：三种方案 | 自动识别 | 动作库丰富 | 细分角度")

# 输入区
user_input = st.text_input("👗 请输入人物/动作/服装/背景描述，不指定就会自行匹配",
                           placeholder="例如：一个女人穿着白色短袖和黑色裙子在户外野餐")

if st.button("🚀 生成 3 套方案", type="primary", use_container_width=True):
    if not user_input:
        st.toast("⚠️ 请先输入描述！")
    else:
        with st.spinner("🧠 视觉总监正在调度机位、挑选动作..."):
            results = generate_three_variations(user_input)

        # 补齐数据，防止API偶尔返回不足3个
        while len(results) < 3:
            results.append("生成失败，请重试")

        st.success("✅ 生成成功！请点击右上角按钮复制。")

        # === 核心布局：三列并排 ===
        col1, col2, col3 = st.columns(3)

        # 方案 1
        with col1:
            st.markdown("### 📸 方案 A")
            st.caption("风格/角度 1")
            # 使用 text 语言模式，配合 CSS 实现自动换行大框
            st.code(results[0], language="text")

        # 方案 2
        with col2:
            st.markdown("### 📸 方案 B")
            st.caption("风格/角度 2")
            st.code(results[1], language="text")

        # 方案 3
        with col3:
            st.markdown("### 📸 方案 C")
            st.caption("风格/角度 3")
            st.code(results[2], language="text")
